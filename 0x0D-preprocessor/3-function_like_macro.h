#ifndef MACROL_H
#define MACROL_H
/**
* Filename: 3-function_like_macro
* @x: input value
* Description: a header file that computes absolute value
*/
#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
